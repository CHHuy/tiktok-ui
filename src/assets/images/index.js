const images = {
    // only with SVG file at the end .default is needed
    logo: require('~/assets/images/logo.svg').default,
    noImage: require('~/assets/images/no-image.png'),
};

export default images;
